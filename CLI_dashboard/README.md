# CLI Dashboard (Standalone Reference)

This standalone folder is provided purely for modular code review and judging. 
The actual running CLI dashboard is natively integrated directly into the main `kraise_system.cpp` executable and runs automatically alongside the sensor processes.
